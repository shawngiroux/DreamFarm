# DreamFarm

`.env` file:
```
export FLASK_APP="run.py"
export FLASK_DEBUG=1
export DISCORD_API_KEY="your key here"

Running:
```
$ pip install pipenv
$ pipenv install
$ source .env
$ pipenv run flask run
```

